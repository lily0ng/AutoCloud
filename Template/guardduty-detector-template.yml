AWSTemplateFormatVersion: '2010-09-09'
Description: 'AWS GuardDuty Detector Template'

Resources:
  GuardDutyDetector:
    Type: AWS::GuardDuty::Detector
    Properties:
      Enable: true
      FindingPublishingFrequency: FIFTEEN_MINUTES

Outputs:
  DetectorId:
    Value: !Ref GuardDutyDetector
